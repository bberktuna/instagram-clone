import React from "react"
import { StyleSheet, Text, View } from "react-native"
import { CustomHeader } from "../../components"

const Followers = () => {
  return (
    <View>
      <CustomHeader headerLeftLabel={username} />
    </View>
  )
}

export default Followers

const styles = StyleSheet.create({})
